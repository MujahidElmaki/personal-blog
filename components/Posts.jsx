import { data } from 'autoprefixer';
import React, { useMemo } from 'react';

import { Post } from './Post';

const Posts = ({ post, hideDate, yearOnly }) => {
  const postsByYear = useMemo(() => {
    const collection = {};
    data.forEach;
  });

  return <div>Posts</div>;
};

export default Posts;
